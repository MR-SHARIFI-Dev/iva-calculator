# Contributing

1. Fork the repository and create a focused branch.
2. Run `npm test` before submitting a pull request.
3. Keep the calculator dependency-free and do not introduce `eval` or `Function`.
4. Add parser tests for every new operator or function.
5. Describe user-visible changes clearly in the pull request.

Bug reports should include the expression, expected result, actual result, browser and operating system.

# گزارش تکمیل IVA Calculator Pro

نسخه تکمیل‌شده: **3.3.0**

## باگ‌های پیدا و رفع‌شده در نسخه 3.3.0 (تأییدشده با تعامل واقعی کاربر در مرورگر)

- **تعارض عدد اویلر و نماد علمی:** فشردن `2`، `e`، `3` به‌جای 2×e×3 مقدار 2000 می‌داد. حالا `e` کوچک همیشه عدد اویلر است و نماد علمی فقط با `E` بزرگ نوشته می‌شود (`2E3` = 2000).
- **توان منفی غیرممکن بود:** بعد از `2^` دکمه منفی غیرفعال می‌شد و کیبورد هم آن را رد می‌کرد. حالا `2^-2` هم با ماوس و هم با کیبورد وارد می‌شود.
- **ضرب ضمنی خاموش بعد از عملگرهای پسوندی:** فشردن `5`، `x!`، `3` نتیجه غلط 360 می‌داد. حالا رقم/ثابت/پرانتز بلافاصله بعد از `!` و `%` نادیده گرفته می‌شود.
- **کلید Space دکمه فوکوس‌شده را دوباره فعال می‌کرد** و کاراکتر تکراری وارد می‌شد.
- **نتایج نماد علمی بعد از `=` قابل ادامه نبودند و نمایش خراب می‌شد** (`1e + 21`). حالا خروجی `1E+21` است و ادامه محاسبه درست کار می‌کند.
- **عدم محافظت localStorage:** در iframe سندباکس‌شده یا حالت خصوصیِ مرورگرها کل برنامه کرش می‌کرد؛ همه دسترسی‌ها اکنون داخل try/catch هستند.
- **تاریخچه بدون اعتبارسنجی خوانده می‌شد**؛ داده خراب می‌توانست رابط کاربری را از کار بیندازد. حالا ورودی‌ها پالایش می‌شوند.
- **نمایش `2pi`** به‌جای `2π` (باگ مرز کلمه در regex) اصلاح شد.
- **کیبورد:** رفتار پیش‌فرض مرورگر برای کلیدهای مدیریت‌شده مسدود شد (مثلاً `/` در فایرفاکس)، کلیدهای `×`، `÷`، `−` پشتیبانی می‌شوند و Space نادیده گرفته می‌شود.
- **لیست سفید شروع عبارت جدید** توابعی مثل `asin`، `abs`، `exp`، `floor` را نمی‌پوشاند؛ با تابع `startsNewExpression` عمومی‌سازی شد.
- **اعشار داخل نماد علمی** (`2E3.`) به‌جای نمایش مرده، رد می‌شود.
- **دکمه‌های عملگر بعد از نقطه اعشار** (`2.` + `+`) که فعال اما بی‌اثر بودند، حالا غیرفعال نمایش داده می‌شوند.

## کارهای انجام‌شده در نسخه‌های قبلی

- پیاده‌سازی Tokenizer و Recursive-Descent Parser بدون `eval` و `Function`
- پشتیبانی از تقدم عملگرها، پرانتز، توان، فاکتوریل و درصد
- توابع علمی، ثابت‌های π و e، مقدار Ans و ضرب ضمنی
- DEG/RAD، پیش‌نمایش نتیجه، خطاهای قابل‌فهم و کنترل کیبورد
- تاریخچه با Local Storage و بازیابی/پاک‌سازی
- طراحی واکنش‌گرا، Dark/Light Mode، دسترس‌پذیری و Reduced Motion
- PWA کامل با Manifest و Service Worker برای اجرای آفلاین
- تست‌های واقعی Parser، قوانین ورودی و مرورگر (Playwright)

## ساختار جدید

فایل خالص `src/format.js` برای قالب‌بندی اعداد و نمایش عبارات اضافه شد تا رفت‌وبرگشت «نتیجه فرمت‌شده → پارسر» با تست تضمین شود. اسکریپت‌های شبیه‌سازی کاربر در `scripts/` قرار گرفتند.

## نتیجه تست

- **۲۳ تست واحد/یکپارچگی** (parser، قوانین ورودی، رفت‌وبرگشت قالب‌بندی، بررسی نسخه) — همه سبز
- **۸ تست واقعی مرورگر** در Chromium شامل تست‌های رگرسیون برای همه باگ‌های بالا — همه سبز
- **۲۱ سناریوی جلسه کاربری** (مثلثات، خطاها، تاریخچه، ریلود، محیط بدون localStorage، موبایل) — همه پاس

برای اجرای دوباره:

```bash
npm install
npx playwright install chromium
npm run verify
```

برای اجرای محلی:

```bash
npm run serve
```

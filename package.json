{
  "name": "iva-calculator-pro",
  "version": "3.3.0",
  "description": "A private, dependency-free scientific calculator with a safe expression parser.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "test:e2e": "playwright test",
    "lint": "eslint src tests sw.js playwright.config.js",
    "verify": "npm run lint && npm test && npm run test:e2e",
    "serve": "python3 -m http.server 4173"
  },
  "engines": {
    "node": ">=20.19"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "eslint": "^10.9.0"
  }
}
